npm init -y
npm i redux
create a file by name step1.js
and change the package.json's main to step1.js
paste this code in step1.js
============================================

let redux = require("redux");
let createStore = redux.legacy_createStore;

// action type
let ADD_HERO = "ADD_HERO";

// action creator
let addHero = () => {
    return {
        type : ADD_HERO
    }
};

// initial State
let initalState = {
    numberOfHeroes : 0
}

// reducer are purefunctions 
let reducer = (state = initalState, action) => {
    switch(action.type){
        case ADD_HERO : return { numberOfHeroes : state.numberOfHeroes + 1 }
        default : return state
    }
}

let store = createStore(reducer);
// initial state in the store
console.log(store.getState());

store.subscribe(() => {
    console.log(store.getState());
});

setInterval(function(){
    store.dispatch(addHero());
}, 1000)
